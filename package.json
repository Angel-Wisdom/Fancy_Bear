{
  "name": "suraksha-2.0",
  "version": "1.0.0",
  "description": "Real-Time Document Anomaly Detection & Fraud Prevention System for Bank Underwriting",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace client\" \"npm run start --workspace server\"",
    "client": "npm run dev --workspace client",
    "server": "npm run start --workspace server",
    "install:all": "npm install && npm install --workspace client && npm install --workspace server",
    "seed": "npm run seed --workspace server",
    "setup": "npm run install:all && npm run seed"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
